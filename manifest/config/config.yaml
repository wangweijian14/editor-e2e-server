server:
  address:     ":8000"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

# Global logging.
logger:
  level : "all"
  stdout: true

# Database.
database:
  logger:
    level:   "all"
    stdout:  false

  default:
    link:   "sqlite:./wiki.db"
    debug:  true

